# Server mode uses the Python cookie-session API by default. Set this only for
# Firebase-only development (for example, a static-build preview).
# VITE_AUTH_MODE=firebase

# Optional credentials used by the USAJobs scanner.
# VITE_USAJOBS_API_KEY=
# VITE_USAJOBS_EMAIL=
