import styles from './Landing.module.css';
export function FavoritesCard({ companies }: { companies: string[] }) { return <section className={`${styles.card} ${styles.favorites}`}><header className={styles.header}><h2>Favorite Companies</h2>{companies.length > 0 && <span>{companies.length} {companies.length === 1 ? 'company' : 'companies'}</span>}</header>{companies.length ? <div className={styles.favoriteList}>{[...companies].sort().map((company) => <span key={company}>♥ {company}</span>)}</div> : <p className={styles.empty}>Star companies in the scanner to add favorites</p>}</section>; }
